for n in range(1,1000): #запускаем цикл
    n2=bin(n)[2:] # П.1: преобразуем десятичную запись числа в двоичную,
                    #убрав при этом первые 2 символа полученной последовательности из-за особенностей языка программирования
    n2=str((n2.count('1'))%2)+n2#П.2: остаток от деления на 2 количества единиц записи приписывается слева к обычной записи
    n2=str((n2.count('1'))%2)+n2#П.3
    r=int(n2,2)#П.4 Переводим полученный результат в десятичную запись
    if r>51: #условный оператор для проверки, больше ли полученный результат, чем 51
        print(n)#печатаем на экран загаданное число
        break #останавливаем цикл в случае, если if сработал

#Ответ 21
